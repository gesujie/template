/* ========================================================================
 * Freeze Table
 * ======================================================================== */

+function($){
  'use strict';

  //Establish Default Settings
  $.fn.freezeTable = function(options){
    var settings = $.extend({

    }, options);
  };


}(jQuery);
